[project]
name = "title_skip"
version = "1.0"
authors = [{ name = "ZetaDaemon" }]
description = "Automatically skips the title screen to go to the main menu."

[tool.sdkmod]
name = "Title Skip"
version = "1.0"

[tool.sdkmod_release_script]
files = ["*.md", "*.py"]
